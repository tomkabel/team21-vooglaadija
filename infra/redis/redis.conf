# Redis configuration for production
# Note: This file is not used directly - docker-compose uses command-line args
# This file is kept for reference and potential future templating needs

bind 0.0.0.0
port 6379
requirepass REDIS_PASSWORD_PLACEHOLDER
appendonly yes
maxmemory 256mb
maxmemory-policy allkeys-lru
