# YouTube Link Processor

Skeleton project generated from `STRUCTURE.md`.

## Quick start

1. Copy `.env.example` to `.env`
2. Install dependencies:
   - `pip install -r requirements.txt`
3. Run API:
   - `uvicorn app.main:app --reload`
4. Run worker:
   - `python -m worker.main`
